[pycodestyle]
# E743 would reject the 'l' register accessor, 'def l'. The rest of the
# list restates the default ignored codes, as pycodestyle offers no way
# to extend them.
ignore = E121,E123,E126,E226,E24,E704,W503,W504,E743
